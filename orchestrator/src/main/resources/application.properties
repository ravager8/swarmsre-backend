spring.application.name=orchestrator
server.port=8081

# Actuator (health check + future metrics)
management.endpoints.web.exposure.include=health,info

# Outbound integrations — where to query when an agent needs metrics.
# Override per environment via SPRING_PROFILES_ACTIVE or env vars.
swarmsre.metrics.prometheus.base-url=http://localhost:9090
